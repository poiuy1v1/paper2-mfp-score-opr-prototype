# Minimal requirements for Supplementary Software 1 for the accompanying manuscript.
# The core pipeline uses only the Python standard library; these packages support
# optional notebook inspection, plotting, and future extension.
numpy>=1.24
pandas>=2.0
matplotlib>=3.7
jupyter>=1.0
