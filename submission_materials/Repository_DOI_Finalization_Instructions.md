# REPOSITORY DOI FINALIZATION INSTRUCTIONS

## Recommended route

1. Create a clean GitHub repository for `SupplementarySoftware1/` and the submission-supporting documentation.
2. Add a final `README.md`, `REPRODUCIBILITY.md`, `CITATION.cff`, `LICENSE`, and release notes.
3. Confirm that `python3 run_all.py` and `python3 validate_outputs.py` pass from a clean checkout.
4. Create a versioned release, for example `v1.0.0`.
5. Archive the release on Zenodo or an equivalent institutional repository.
6. Insert the versioned DOI into the manuscript Data/code availability statement, cover letter, `CITATION.cff`, and repository README.

## Placeholder to replace

`[repository DOI to be inserted before final submission]`

## Do not insert

Do not invent a DOI. Only insert the DOI after the repository release is archived.
